import { BorderGradientAnimatedDirective } from './border-gradient-animated.directive';

describe('BorderGradientAnimatedDirective', () => {
  it('should create an instance', () => {
    const directive = new BorderGradientAnimatedDirective();
    expect(directive).toBeTruthy();
  });
});
